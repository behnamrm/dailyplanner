import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.behnamrmz.dailyplanner',
  appName: 'Daily Planner',
  webDir: 'dist'
};

export default config;
